import { AccordionItem, Link } from '@nextui-org/react'

import { NavigationItemType } from '../../model'

interface Props {
  item: NavigationItemType
  subItem?: boolean
}

export const NavigationLink = ({ item, subItem }: Props) => {}
